## Data

Raw dataset (too large for Git) must be placed here:

- data/complaints.csv

Generated by notebooks:
- data/complaints_clean.parquet (full cleaned dataset, not committed)
- data/complaints_sample.parquet (dev sample, can be committed if small)

If you use Kaggle, download the dataset and rename it to `complaints.csv`.